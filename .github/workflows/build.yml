name: build
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install dependencies
        run: |
          sudo apt-get -y update
          sudo apt-get -y install cython3 libwings-dev
      - name: Build
        run: |
          python3 setup.py test
